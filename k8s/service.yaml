apiVersion: v1
kind: Service
metadata:
  name: hello-platform
  namespace: hello-platform
spec:
  type: NodePort
  selector:
    app: hello-platform
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
